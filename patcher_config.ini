; Gen 3 Shiny Odds Patcher config
; Set shiny odds as 1 in N (integer > 0)
; Modes:
; - auto/canonical/reroll: canonical PID rerolls (PKHeX-valid shinies)
; - native/legacy: older threshold patch behavior (non-canonical at high rates)
;
; Example values:
; odds=8192
; odds=4096
; odds=256
; odds=16
; mode=auto
; mode=canonical
; mode=reroll
; mode=native
; mode=legacy
odds=16
mode=auto
